# 翻页数字时钟

一个基于Canvas实现的精美翻页数字时钟应用，具有真实的卡片翻页动画效果和多种主题选择。

## 功能特性

- **真实翻页动画**：模拟物理卡片翻页效果，数字变化时呈现流畅的3D翻转动画
- **多种主题**：内置经典、暗黑、明亮、海洋、日落等多种主题
- **自定义主题**：支持自定义前景色、背景色、数字色和分隔符色
- **响应式设计**：自适应不同屏幕尺寸
- **实时更新**：每秒自动更新时间显示

## 技术实现

- **Canvas渲染**：使用HTML5 Canvas进行高性能绘制
- **3D翻转动画**：通过缩放变换模拟3D透视效果，实现真实的卡片翻页
- **模块化架构**：代码结构清晰，逻辑分离，易于维护和扩展

## 安装与运行

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

### 构建生产版本

```bash
pnpm build
```

### 预览生产版本

```bash
pnpm preview
```

## 使用说明

1. 打开应用后，您将看到一个精美的翻页数字时钟
2. 使用顶部的主题选择器切换不同的视觉主题
3. 选择"自定义"主题后，可以通过颜色选择器调整各个元素的颜色
4. 观察数字变化时的流畅翻页动画效果

## 项目结构

```bash
├── index.html          # 主HTML文件
├── main.js             # 主要JavaScript逻辑
├── style.css           # 样式文件
├── package.json        # 项目配置
└── readme.md           # 项目说明文档
```

## 核心实现

### 翻页动画原理

翻页动画分为两个阶段：

1. **前半段（0-50%）**：旧数字的上半部分沿中轴线下翻，逐渐消失，同时露出新数字的上半部分
2. **后半段（50-100%）**：新数字的下半部分沿中轴线下翻，逐渐显示，同时遮盖旧数字的下半部分

通过Canvas的缩放变换和渐变阴影效果，模拟出真实的3D翻页视觉效果。

### 时间更新机制

- 使用 `setTimeout` 每秒更新一次时间
- 统一管理当前显示时间，确保时间显示的一致性
- 检测数字变化，只对变化的数字触发翻页动画

## 许可证

MIT License
